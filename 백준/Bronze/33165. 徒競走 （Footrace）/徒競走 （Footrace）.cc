#include <iostream>
using namespace std;

int main() {
    int T, V;
    cin >> T >> V; // 시간(T)과 속도(V) 입력

    int distance = T * V; // 거리 계산

    cout << distance << endl; // 결과 출력

    return 0;
}
